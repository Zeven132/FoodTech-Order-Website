var formattedIngredient = new Array(20); // maybe 2d array so that it can be for each catagory of food

function GetClassInfo()
{

}

// ask user for quantity (5 cups, 400ml, 3, etc)
// then the ingredient
// add identifiable characters between elements
function GetIngredient() 
{
    let quantity = ""; //user input
    let ingredient = ""; //user input

    //formattedIngredient += quantity+">>"+ingredient+"||";
}

function DisplayData()
{
    //display all the data
    // at || do <tr>
    // at >> do <th>
}

